# 复制为 .env 并填入真实值。.env 永远不进 git（已在 .gitignore 中）。

# LLM 路由（模型 alias 声明在 config/models.yaml；key 仅经环境注入，禁止入库/入仓）
OPENAI_API_KEY=replace-me
ANTHROPIC_API_KEY=replace-me

# Langfuse 观测（ops/langfuse/）
LANGFUSE_HOST=replace-me
LANGFUSE_PUBLIC_KEY=replace-me
LANGFUSE_SECRET_KEY=replace-me
